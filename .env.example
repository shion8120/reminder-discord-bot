# 通知先の Discord チャンネルの Webhook URL。未設定ならコンソールに出す
DISCORD_WEBHOOK_URL=

# 任意。YouTube Data API v3 のキー。あると RSS より安定して取れる（無料枠で足りる）
YOUTUBE_API_KEY=

# 確認間隔（分）
POLL_MINUTES=30

# 初回起動時に通知する範囲（日）。これより古い動画は既読扱い
INITIAL_LOOKBACK_DAYS=3

# Render では /var/data（Persistent Disk）
BOT_DATA_DIR=
